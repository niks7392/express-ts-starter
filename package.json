{
  "name": "mern-app-template",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"ts-node-dev --respawn src/index.ts\" \" npm run dev:client\" ",
    "dev:client": "cd client && npm run dev",
    "build": "npm install && npm tsc",
    "start": "cd client && npm run build && cd .. && tsc && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.6",
    "ts-node-dev": "^2.0.0"
  }
}
